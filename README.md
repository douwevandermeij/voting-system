# Voting System

Requirements:

* Python 3.8
* Pipenv

Install:

    pipenv install --dev

Run:

    pipenv run python main.py

Or first start `pipenv shell` and just run:

    python main.py

Run tests (in `pipenv shell`:

    pytest
